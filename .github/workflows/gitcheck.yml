name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: set up
      run: mkdir build && cd build
    - name: cmake
      run: cmake ..
    - name: make check
      run: make check
   # - name: make distcheck
    #  run: make distcheck
